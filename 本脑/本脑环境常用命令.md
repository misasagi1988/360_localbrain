# 本脑环境常用命令

标签（空格分隔）： 360BrainSecurity

---
mvn出包：
mvn clean package -DskipTests

gradle出包：
./gradlew clean build -x test

es dsl查询demo
```
{
  "query": {
    "bool": {
      "must": [
        {
          "term": {
            "metricset.module": {
              "value": "elasticsearch"
            }
          }
        }
      ]
    }
  },
  "sort":[{"@timestamp":{"order":"desc"}}]
}
```

docker 更新文件cmd: 
bash /opt/qihoo/soc/tools/docker_tools/replace_and_restart_service.sh tomcat /home/mengyujing/common-5.0.jar /opt/qihoo/soc/tomcat/webapps/ROOT/WEB-INF/lib/common-5.0.jar

bash /opt/qihoo/soc/tools/docker_tools/replace_and_restart_service.sh tomcat  /home/elasticsearch/system-5.0.jar /opt/qihoo/soc/tomcat/webapps/ROOT/WEB-INF/lib/system-5.0.jar

docker 无entrypoint执行镜像，并做操作，比如查看entrypoint.sh脚本内容，demo:
docker run --rm --entrypoint="" lbrain-nginx:latest cat /docker-entrypoint.sh > docker-entrypoint.sh


统计当前目录下所有文件夹的大小，按大小排序，取前10
du -h --max-depth=1 | sort -hr | head -n 10
获取文件夹大小
du -sh /path/to/folder

本脑不登录发请求：
curl -H "x-api-key:f3b79df85273b7b84fd206d8731a8424" "http://127.0.0.1:7001/__internal/system/config/syslog"
curl -X POST -H 'Content-Type: application/json' -H "x-api-key:f3b79df85273b7b84fd206d8731a8424" -d '{"status":true,"type":"kafka","protocol":"UDP","source":"log","halite":"事件名称 = \"网络攻击\"","excludeFields":["源端口"],"kafkaConfigs":[{"servers":"127.0.0.1:9092","topic":"event"}]}' "http://127.0.0.1:7001/__internal/system/config/syslog"

# hql相关
where, 过滤条件子句
`==`，判断是否相等，支持单值或数组，demo:
index == "alarm_merge" and dev_ip_list == "127.0.0.1"
any_match, 是否匹配数组中的某个值，demo:
index == "artifact_entity_intranet_ip" and (asset_name != '' and asset_name != null) and any_match(risk_level, [1, 2, 3, 4])
ip_in, ip过滤，demo: 
index == "alarm_merge" and (victim_array.ip belong 内网IP or ip_in(victim_array.ip, "192.168.3.1,192.168.1.3"))
belong, 信息组，demo: 
index == "alarm_merge" and (victim_array.ip belong 内网IP or ip_in(victim_array.ip, "192.168.3.1,192.168.1.3"))
fields, 提前字段相关
index == "asset" | fields ip_address, risk_score, importance
stats，统计相关，可以添加分组字段，统计类型：count(计数), dc(去重计数), sum(求和), values(去重列表) avg, max,min,range, estdc
index == "alarm_merge"  | stats count(id) as cnt by alarm_level
head，按搜索顺序返回前 N 个指定结果。
top, 显⽰字段最常⻅的值。若包含可选的 by-clause，该命令将查找groupby字段值分组最常⽤的值。
sort, 按指定字段对所有结果进⾏排序。减号 (-)表⽰降序，加号 ( + ) 表⽰升序。demo:
index == "alarm_merge"  | sort -update_time

# 统计函数

|   |   |   |
|---|---|---|
|avg(X)|返回字段 X 中的值的平均值。|聚合函数|
|count(X)|返回包含任意值(非空)的您指定的字段的出现次数。例如:count(id)。|聚合函数|
|dc(X)|返回字段 X 中非重复值的计数，目前也已estdc方案实现。|聚合函数|
|estdc(X)|返回字段 X 中非重复值的估计计数。|聚合函数|
|max(X)|返回字段 X 的最大值。|聚合函数|
|min(X)|返回字段 X 的最小值。|聚合函数|
|range(X)|返回字段 X 的最大值与最小值之差(前提是 X 的值为数 字)。|聚合函数|
|sum(X)|返回字段 X 的值的总和。|聚合函数|

# dv相关

获取dv worker metrics写成功信息，tailf-metric.sh脚本，第一个参数是采集器数据源名称
bin/tailf-metric.sh /opt/qihoo/soc/dataviewer/worker/logs/metric.log test read_count  'writer_success_count(Enterprise-ES)'  'writer_success_count(Enterprise-SAE-KAFKA)'

# es相关

POST http://localhost:9200/your_index_name/_delete_by_query 
{ "query": { "match_all": {} } }

# docker之后

## 基础命令

查看docker镜像: 
docker ps -a

进入某个名称(secstudy_rule_evaluation)的docker镜像: 
docker exec -it secstudy_rule_evaluation sh

数据库操作: 
docker exec -it lbrain-mysql mysql -h 127.0.0.1 -P 3399 -usoc -p'Q!hooS0c'

查看防火墙策略:
iptables -L -n

防火墙服务操作:
systemctl status lbrain-iptables

添加防火墙策略举例:
iptablesctl -A INPUT -p tcp -s 11.43.177.228 --dport 7848 -j ACCEPT
iptablesctl -A INPUT -p tcp --dport 3399 -j ACCEPT

容器内网络工具:
可通过/opt/qihoo/soc/tools/docker_tools/net-tool.sh脚本查看容器内网络情况
demo: ./net-tool.sh lbrain-webserver ping www.baidu.com

## 加密解密工具
docker exec lbrain-enc bash enc.sh -d '3Kq/WoeVOgjHBBs0B+RCBQ=='
docker exec lbrain-enc bash -c 'enc.sh -e 'Q!hooS0c'

## 开发换包操作指南

### 复制文件到容器中

参考命令：docker cp /opt/file.txt lbrain-tomcat:/opt/qihoo/soc/file.txt

/opt/file.txt为宿主机上文件，lbrain-tomcat为容器名称，/opt/qihoo/soc/file.txt为容器内目标文件

### 进入容器确认文件是否放到所需位置

参考命令：docker exec -it lbrain-tomcat bash

### 修改镜像的老的tag

docker tag lbrain-tomcat:3.5 lbrain-tomcat:3.5bak

如果有多个中间版本，建议删除，否则会占用存储，命令：

docker rmi lbrain-tomcat:3.5bak

### 保存容器修改到镜像

参考命令：docker commit lbrain-tomcat lbrain-tomcat:3.5

lbrain-tomcat为容器名， 3.5为镜像tag(启动脚本里使用了此tag，所以最新应用的使用这个tag)

### 重启服务

socctl restart tomcat

### 脚本help

/opt/qihoo/soc/tools/docker_tools/replace_and_restart_service.sh   tomcat  /opt/system-0.0.1-SNAPSHOT.jar  /opt/qihoo/soc/tomcat/webapps/ROOT/WEB-INF/lib/

##  java模块远程调试(以tomcat为例)

### 添加调试参数

在tomcat目录下找到启动脚本：/opt/qihoo/soc/tomcat/tomcat_start.sh

在启动脚本中添加t调试启动参数

### 重启服务

socctl restart tomcat

执行socctl update不会生效，因为没有修改模块对应的ini文件

### 开始远程调试


# docker前本脑常用命令
### arthas

doc: https://arthas.aliyun.com/doc/
java -jar arthas-boot.jar --target-ip remote-ip, 默认端口8563，target用于远程连接
常用命令：dashboard, thread, jvm, profiler
,cls, quit(退出当前arthas客户端), stop(关闭 Arthas 服务端)
**dashboard**: 当前系统的实时数据面板，按 ctrl+c 退出。默认5s刷新一次。
**thread**: 查看当前线程信息，查看线程的堆栈。
demo: thread -n 3

| 参数     | 参数说明   | 
| --------  | ----- | 
| id     | 线程id |
| [n:]   | 指定最忙的前N个线程并打印堆栈 |
| [b]    | 找出当前阻塞其他线程的线程  |
**monitor**: 对匹配 class-pattern／method-pattern／condition-express的类、方法的调用进行监控。
demo: monitor -c 5 demo.MathGame primeFactors

| 参数     | 参数说明   | 
| --------  | ----- | 
| [c:]     | 统计周期，默认值为120秒 |
监控的维度：

| 监控项     | 说明   | 
| --------  | ----- |
| timestamp     | 时间戳   | 
| class     | Java类   | 
| method     | 方法（构造方法、普通方法）   | 
| total	     | 调用次数   | 
| success     | 	成功次数   | 
| fail	     | 失败次数   | 
| rt	     | 平均RT   | 
| fail-rate	     | 失败率   | 
**watch**: 函数执行数据观测，能观察到的范围为：返回值、抛出异常、入参
demo: watch com.espertech.esper.epl.expression.ops.ExprRegexpNode evaluate '{params,returnObj}'  '#cost>5' -n 5  -x 3 

| 参数     | 参数说明   | 
| --------  | ----- | 
| class-pattern   | 	类名表达式匹配  |
| method-pattern   | 	函数名表达式匹配  |
| express	   | 观察表达式，默认值：{params, target, returnObj}  |
| condition-express   | 	条件表达式  |
| [b]   | 	在函数调用之前观察  |
| [e]   | 	在函数异常之后观察  |
| [s]   | 	在函数返回之后观察  |
| [f]   | 	在函数结束之后(正常返回和异常返回)观察  |
| [E]   | 	开启正则表达式匹配，默认为通配符匹配  |
| [x:]   | 	指定输出结果的属性遍历深度，默认为 1  |
特别说明：
watch 命令定义了4个观察事件点，即 -b 函数调用前，-e 函数异常后，-s 函数返回后，-f 函数结束后
4个观察事件点 -b、-e、-s 默认关闭，-f 默认打开，当指定观察点被打开后，在相应事件点会对观察表达式进行求值并输出
这里要注意函数入参和函数出参的区别，有可能在中间被修改导致前后不一致，除了 -b 事件点 params 代表函数入参外，其余事件都代表函数出参
当使用 -b 时，由于观察事件点是在函数调用前，此时返回值或异常均不存在
在watch命令的结果里，会打印出location信息。location有三种可能值：AtEnter，AtExit，AtExceptionExit。对应函数入口，函数正常return，函数抛出异常。
**trace**: 方法内部调用路径，并输出方法路径上的每个节点上耗时
demo: trace demo.MathGame run
**stack**: 输出当前方法被调用的调用路径
demo: stack demo.MathGame primeFactors
**profiler**: 使用async-profiler生成火焰图
profiler start/status/stop


### Firewall

 - 开启防火墙: systemctl start firewalld.service
 - 关闭防火墙: systemctl stop firewalld.service
 - 查看防火墙状态: firewall-cmd --state
 - 针对某个ip开放端口: 
 firewall-cmd --permanent --add-rich-rule="rule family="ipv4" source address="192.168.142.166" port port="6379" protocol="tcp" accept"
 firewall-cmd --permanent --add-rich-rule="rule family="ipv4" source address="192.168.0.233" accept"
 - 删除某个ip: firewall-cmd --permanent --remove-rich-rule="rule family="ipv4" source address="192.168.1.51" accept"
 - 重载: firewall-cmd --reload

### kafka
qihoosoc-sae-88.88.88.56:8765 
 - 消费数据
bin/kafka-console-consumer.sh --bootstrap-server localhost:9092 --topic alarm --from-beginning --property print.key=true
 - 查看topic偏移量
bin/kafka-run-class.sh kafka.tools.GetOffsetShell --topic hes-sae-group-0  --time -1 --broker-list localhost:9092
bin/kafka-consumer-groups.sh --bootstrap-server 127.0.0.1:9092 --group qihoosoc-sae-127.0.0.1:8765 --describe   artifact-event-group 
 - 查看各个topic的各个group的消费情况，offset，lag...
bin/kafka-consumer-groups.sh --bootstrap-server 127.0.0.1:9092 --all-groups --describe
bin/kafka-consumer-groups.sh --bootstrap-server 127.0.0.1:9092 --describe --group incident-subscriber --members
shell脚本，参数为topic名称
topic=$1
for i in `/opt/qihoo/soc/kafka/bin/kafka-consumer-groups.sh --list --bootstrap-server localhost:9092`;
do echo ${i};
(/opt/qihoo/soc/kafka/bin/kafka-consumer-groups.sh --describe --bootstrap-server localhost:9092 --group ${i} 2>&1 | grep ${topic});
done
- 生产数据 
bin/kafka-console-producer.sh --bootstrap-server localhost:9092 --topic hes-sae-group-0

### mysql

连接数据库：./mysql -h 127.0.0.1 -P 3399 -usoc -p'Q!hooS0c' soc
```sql
select id, rule_name, sim_name , JSON_EXTRACT(raw, '$.alert.alarmKey') from sae_rule where sim_name in ("主机异常","进程异常","脚本异常","驱动异常","容器异常","恶意程序","漏洞利用","文件异常","注册表异常","网络异常") and JSON_EXTRACT(raw, '$.alert.alarmKey')  like "%src_address%";
update sae_rule set raw = JSON_REPLACE(raw, '$.alert.alarmKey', json_array()), alarm_attr = JSON_REPLACE(alarm_attr, '$.alarmKey', json_array());

select T1.*, T2.threat_type as threat_type from security_event_base T1 left outer join (select event_id, GROUP_CONCAT(threat_type) as threat_type from event_threat_type_ref group by event_id) T2 on T1.id = T2.event_id ORDER BY id DESC

select T1.*, T2.threat_type as threat_type, T3.operation_type as operation_type from security_event_base T1 left outer join event_threat_type_ref T2 on T1.id = T2.event_id left outer join event_operation_type_ref T3 on T1.id = T3.event_id ORDER BY id DESC

select T1.*, T2.threat_type as threat_type, T3.operation_type as operation_type from (
        select * from security_event_base where id = #{id}) T1 left join event_threat_type_ref T2 on T1.id = T2.event_id left join event_operation_type_ref T3 on T1.id = T3.event_id

```

### redis

连接redis：./redis-cli -h 127.0.0.1 -p 6379 -a cloud@qihoo.com

### DM达梦数据库
连接：/opt/dm8/bin/disql soc/'Q!hooS0c'
执行sql脚本：/opt/dm8/bin/disql soc/'Q!hooS0c' \`/opt/test.sql
自动刷新：
SET AUTO ON
SET DEFINE OFF

### Zookeeper 连接

export CLIENT_JVMFLAGS="-Dzookeeper.clientCnxnSocket=org.apache.zookeeper.ClientCnxnSocketNetty -Dzookeeper.client.secure=true -Dzookeeper.ssl.keyStore.location=/opt/qihoo/soc/etc/ssl/client_keystore.jks -Dzookeeper.ssl.keyStore.password=qihoosoc -Dzookeeper.ssl.trustStore.location=/opt/qihoo/soc/etc/ssl/client_truststore.jks -Dzookeeper.ssl.trustStore.password=qihoosoc -Dzookeeper.ssl.hostnameVerification=false"
然后到zookeeper安装目录下，执行bin/zkCli.sh
 - ls：查看指定路径下包含的节点
 - create：创建一个节点
 - get：显示指定路径下节点的信息
 - set：设置节点的内容
 - delete：删除一个节点

### Zookeeper分布式历史任务排查思路：

- mysql查看历史任务状态
select id, status where name = "taskName";
id表示taskId, status状态说明： -1表示暂停；0表示执行完毕；1表示正在运行

- 连接zookeeper，确认历史任务放入zk
cmd demo(keyStore.location/trustStore.location根据环境替换):
export CLIENT_JVMFLAGS="-Dzookeeper.clientCnxnSocket=org.apache.zookeeper.ClientCnxnSocketNetty -Dzookeeper.client.secure=true -Dzookeeper.ssl.keyStore.location=/opt/qihoo/soc/etc/ssl/client_keystore.jks -Dzookeeper.ssl.keyStore.password=hansight -Dzookeeper.ssl.trustStore.location=/opt/qihoo/soc/etc/ssl/client_truststore.jks -Dzookeeper.ssl.trustStore.password=hansight -Dzookeeper.ssl.hostnameVerification=false"
zookeeper安装目录下，执行bin/zkCli.sh
历史任务path: /sae_distribution/history-task
ls /sae_distribution/history-task： 获取历史任务id列表
get /sae_distribution/history-task/taskId/core: 得到执行历史任务的core的信息

- 查看sae相关日志：
scramble.log: 
检测到历史任务: add new history task, task id: {taskId}
准备环境失败: prepare env for history task {taskId} failed.
准备成功后开始执行历史任务: processing history task: {taskId}
sae-core.log: 
历史任务执行中: history task:{taskName} has analysed {analysisCount} datas

- 历史任务配置的注意点
  1. 时间范围不要太大
  2. 利用历史查询条件及HQLite构造filter语句，减小进入引擎的数据量，提高处理速度
  3. 任务使用的规则，建议配置多个，效率与单个而言差别不大，但不用频繁重复读取es，因此效果会好一些
  4. 任务不要频繁创建，频繁删除

### linux
#### centos查看系统配置信息
linux查看版本当前操作系统发行信息 cat /etc/centos-release
查看内核版本uname -a或者cat /proc/version
查看 CPU 物理个数 grep 'physical id' /proc/cpuinfo | sort -u | wc -l
查看 CPU 核心数量 grep 'core id' /proc/cpuinfo | sort -u | wc -l
查看 CPU 线程数 grep 'processor' /proc/cpuinfo | sort -u | wc -l
查看 CPU 的详细信息 cat /proc/cpuinfo
查看内存情况free -g  (-g对应GB,-m对应mb)
查看硬盘使用情况df -lh

#### 查询进程内存
ps aux|sort -k4,4nr|head -n 10 查看内存占用前10名的程序 
ps -aux|grep xxx

#### 文件相关
du -sh xxx, 统计文件夹大小
du -h –max-depth=1 *，可以查看当前目录下各文件、文件夹的大小
du -h –max-depth=0 *，可以只显示直接子目录文件及文件夹大小统计值

#### curl
curl -X POST -H "Content-Type: application/json" http://localhost:8080/sae/api/cep/rules -d '{json_body}'

curl请求：查看总耗时、连接耗时、响应耗时等核心指标 
curl -o /dev/null -s -w " 
=== 请求耗时统计 ===
总耗时: %{time_total} 秒 
建立连接耗时: %{time_connect} 秒 
TCP握手耗时: %{time_appconnect} 秒 
等待响应耗时: %{time_starttransfer} 秒 
响应传输耗时: %{time_total} - %{time_starttransfer} = $(echo "%{time_total} - %{time_starttransfer}" | bc ) 秒 
HTTP状态码: %{http_code} 
" "http://127.0.0.1:8080/sae/api/cep/rules/baseline/config"

####清理缓存
echo 3 > /proc/sys/vm/drop_caches

#### scp
从本地将文件传输到服务器
scp /Users/mac_pc/Desktop/test.png root@192.168.1.1:/root
从本地将文件夹传输到服务器
scp -r /Users/mac_pc/Desktop/test root@192.168.1.1:/root
将服务器上的文件传输到本地
scp root@192.168.1.1:/data/wwwroot/default/111.png /Users/mac_pc/Desktop
将服务器上的文件夹传输到本地
scp -r root@192.168.1.1:/data/wwwroot/default/test /Users/mac_pc/Desktop
demo: 
scp -r /home/mengyujing/com elasticsearch@10.225.8.238:/home/elasticsearch

#### ps&top
ps -eo pid,lstart,etime | grep <pid>， 查看进程启动时间，运行时长
top -H -p <pid>, 查看进程内各个线程的运行情况

#### vmstat
在 Linux 系统下，可以使用 Linux 内核提供的 vmstat 命令，vmstat是一款指定采样周期和次数的功能性监测工具，我们可以使用它来监视 Java 程序运行过程中系统的上下文切换频率。
vmstat 1 3 命令行代表每秒收集一次性能指标，总共获取3次。

 - procs
r：等待运行的进程数
b：处于非中断睡眠状态的进程数
 - memory
swpd：虚拟内存使用情况
free：空闲的内存
buff：用来作为缓冲的内存数
cache：缓存大小
 - swap
si：从磁盘交换到内存的交换页数量
so：从内存交换到磁盘的交换页数量
 - io
bi：发送到块设备的块数
bo：从块设备接收到的块数
 - system
in：每秒中断数
cs：每秒上下文切换次数
 - cpu
us：用户CPU使用时间
sy：内核CPU系统使用时间
id：空闲时间
wa：等待I/O时间
st：运行虚拟机窃取的时间
备注：
如果r经常大于4，id经常少于40，表示cpu的负荷很重。
如果bi，bo长期不等于0，表示内存不足。
如果disk经常不等于0，且在b中的队列大于3，表示io性能不好。

pidstat命令可以帮助我们监测到进程的具体线程的上下文切换。
常用参数：
-u：默认参数，显示各个进程的cpu使用情况；
-r：显示各个进程的内存使用情况；
-d：显示各个进程的I/O使用情况；
-w：显示每个进程的上下文切换情况；
-p：指定进程号；
-t：显示进程中线程的统计信息

查看具体线程的上下文切换异常，我们还可以使用jstack命令查看线程堆栈的运行情况。jstack是JDK自带的线程堆栈分析工具，使用该命令可以查看或导出 Java 应用程序中的线程堆栈信息。
jstack最常用的功能就是使用 jstack pid 命令查看线程堆栈信息，通常是结合pidstat -p pid -t一起查看具体线程的状态，也经常用来排查一些死锁的异常。

### jvm

#### jps
jps -l
查看java虚拟机启动使用的参数
java -XX:+PrintCommandLineFlags -version

#### jcmd
jcmd PID VM.flags/VM.command_line

#### jstat

jstat -options 可以列出当前JVM版本支持的选项，常见的有
-class (类加载器) 
-compiler (JIT) 
-gc (GC堆状态) 
-gccapacity (各区大小) 
-gccause (最近一次GC统计和原因) 
-gcnew (新区统计)
-gcnewcapacity (新区大小)
-gcold (老区统计)
-gcoldcapacity (老区大小)
-gcpermcapacity (永久区大小)
-gcutil (GC统计汇总)
-printcompilation (HotSpot编译统计)
demo: jstat -gccause pid 3000

#### jstack
cat jstack.log | grep "java.lang.Thread.State" | sort -nr | uniq -c，对jstack的线程不同状态有一个整体的把握
#### jinfo

#### jmap
jmap -heap pid
jmap -histo:live pid
jmap -dump:live,format=b,file=dump.hprof pid

