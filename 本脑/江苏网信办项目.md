# 江苏网信办项目

标签（空格分隔）： 360BrainSecurity

---



数据治理-告警数据梳理-v1: 信工所的数据，已确定，不能动，可扩充
数据治理-探针数据梳理-v2: 探针数据，可自定义规范，已有字段需复用不可改动
数据治理-数据梳理-v5-4-DNS_v3：已确定，不能动，可扩充


**<font color="Red">Q&A</font>**
- 时间格式及单位能否统一，日期时间型，格式采用YYYYMMDDhh24mmss，数据中是字符串("20220304151030")这种？还是整形时间戳格式？建议采用epoch_millis格式。
- 逗号分隔字符串可否用数组来替代，标准中支持数组：数组是一系列类似数据的集合
- 必备字段，哪些不给sae分析
- sae自己做全局白名单过滤
- 域名、url威胁情报匹配
- 有些字段不存在(client_host_sign等)
- kafka partittion数量 数据加密？

- 时间?：
发生时间: foundTime	日期时间型	格式采用YYYYMMDDhh24mmsss，精确到**毫秒**
开始时间: startTime	日期时间型	格式采用YYYYMMDDhh24mmss，精确到**秒**
结束时间: endTime	日期时间型	格式采用YYYYMMDDhh24mmss，精确到**秒**
start_time/end_time ice当作long处理的
AlarmEngineProxy类

- 数组类型转换?:
攻击者组attacker_array/受害者组victim_array
目标对象IP: dstIp, 字符型, 受攻击对象的IP地址，支持 ipv4、ipv6 格式， 多个时用英文逗号隔开，最大范围存储20个目标对象IP
攻击者IP: srcIp	字符型	攻击者或仿冒者的IP地址 支持 ipv4、ipv6 格式, 多个时用英文逗号隔开, 最大范围存储20个目标对象IP
dstDomain：受攻击对象的域名，多个时用英文逗号隔开，最大范围存储20个目标对象域名。情报匹配逻辑要改动？url呢？

- 是否支持数组：
字段原来是数组，现在变成字符串逗号分隔了，有些功能有问题(ice-web MergeAlarmService.java)
ice配置文件：影响，incident-field-extractor.victim_array
重保中心、合并告警一块接口逻辑有影响


- 字段不存在:
client_host_sign //主机ID
attack_id //ATTCK_ID
host_name //主机名称
host_ip //主机IP
parse_error
receive_time(ice, SearchTaskHelper)


SAE额外工作：
DV不写kafka情况：
- 命中全局白名单(ip/domain/url/account)
- 时间偏移过大: 发生时间超前10min，或滞后2h
- 解析错误 || 必备字段缺失: event_name


