# 一体机需求

标签（空格分隔）： 一体机

---

## L4.0后续版本todo list
告警&安全事件自动处置优化，合入本脑对应功能，注意考虑升级，系统配置数据结构有变化
云脑规则


## 需求

- 本脑移植项
数据解析优化
dv改动，发到kafka的数据带key，所有数据都发到kafka，sae自行过滤有用数据
内部事件逻辑优化，写入特定索引
nacos鉴权
spring boot版本升级
ipv6
双子星模式改造，规则下发
搜索告警支持告警静默
starter-dependencies升级为brain-2.5
多维信息组，操作符inmap, not_inmap
事件名称字段改名字

- 告警通知：
XDR规则告警如何通知？用户不可查看及修改，只允许启停。不通知
通知limiter？soar
通知时间？soar
是否需要条件？No
通知模板？自己定
通知方式？
钉钉：desc，subject，message，from，to
短信：desc，subject，message，from，to
邮件：desc，subject，message，from，to
企业微信
邮件、短信、钉钉、企业微信、Webhook

- 技术调研
流量和终端的数据如何整合？
如何根据流量和终端的数据获取终端的进程信息？通过流量五元组、终端数据五元组，拿到终端，获取进程父进程信息？
1. 被动资产发现技术调研；背景：不接天相时，我们的环境没有任何资产数据，相对竞品缺失的功能。
  目前了解的情况：
  天相：主动资产发现
  如果对接了天相，本脑的资产数据全部来自于天相。
  artifact：被动资产发现，基于目的地址从AISA日志中提取资产信息(app_name or os is not null)。
  暂时只写入artifact内网IP对应实体索引，没有对其做进一步操作。如果对接了天相，天相会定时调用资产/discover接口，获取这部分数据，去判断是否是资产，如果是，把资产信息推送给本脑。
  这部分我们可以做点什么吗？基于流量检测感知内网资产？单纯基于流量够吗？EDR一类的日志、漏扫设备的数据呢？比如用户登录这种事件，可以拿来分析。
<font color='red'>**这部分应该只是一体机的需求。**</font>
20220804进一步讨论：
一体机本脑对接的3个主要XDR类型：**AISA(流量), EPP/EDR(终端), BDR(终端)**
在XDR背景下，如果对接了终端一类的产品，这些XDR终端设备会同步资产信息，可以直接导入他们扫到的资产设备，到我们的环境中。这是一种实现方式。这个对本脑是有意义的(可能有意义，前提是未对接天相，或者本脑做下改造)，一体机几乎不对接任何终端设备，所以一体机这条路是走不通的。
想到的方法是：被动+主动相结合
 - 分析AISA数据，关注其中的协议、目的地址、目的端口等，根据协议获取相关的服务信息
 - 类似天相做法，主动探测，判断该目的地址是否是资产
 - IP是动态变化的，尤其针对DHCP，这种怎么做？
 - AISA数据，何时能吐出mac地址？
 - 以ip为标识？ip和mac通过ARP/RARP互转。网关服务器呢？
 - attention: 流量看到的ip和终端的实际ip可能没有关系：过网关...
主流的资产探测方式为主动发现的方式，该方法通过模拟资产间的交互过程，向目标资产发送探测报文，根据回复的响应内容，提取资产信息。
主动探测可借助于nmap工具，它具备服务和版本探测、操作系统探测的功能。
目前看到的AISA数据，解析规则解析做的不好，请求头、请求内容、响应头、响应内容没有区分开来，有些server没有解析。
<font color='red'>**最终方案：**</font>
AISA: 使用artifact做被动资产发现
EPP/EDR/BDR:  定时调用相应产品的接口全量爬取资产数据

2. 流量和终端的关联技术调研；找出一个可行的方案，让主机能够和IP关联起来。技术实现
    关联流量和终端只能用源地址、目的地址、主机ip和mac地址关联
  安全事件  合并告警  跟资产关联，利用资产的扩展ip。sae这边也可以做这块。
  attention: 流量看到的ip和终端的实际ip可能没有关系：过网关...

3. 资产管理：流量和终端的数据拉通、整合；目前资产只能完全是以IP为维度，现在流量和终端同时存在的时候，无法进行整合。系统实现：dv/sae/ice哪边做丰富化?
   我们对现有资产类型的定义？服务器、数据库、中间件、网络设备？我们会对接一些漏洞扫描工具吗？

Qradar资产关注字段：MAC address, a NetBIOS host name, a DNS host name, IP address
MAC addresses, NetBIOS host names, and DNS host names是唯一的
资产数据可来源于event/flow/vul-scanner/user-interface中。
针对数据丰富化：目前不建议。
dv有丰富化，源地址/目的地址/主机ip，只丰富化个别字段
其他像告警、合并告警、安全事件，都是前端做丰富化。
优先依据mac，如果没有mac，用ip
一个设备通常只有一个ip，或者多个ip在不同网段。资产的扩展ip列表有何用？

网络设备的MAC地址是全球唯一的。MAC地址长度为48比特，通常用十六进制表示。MAC地址包含两部分：前24比特是组织唯一标识符(OUI，OrganizationallyUniqueIdentifier)，由IEEE统一分配给设备制造商。
设备的唯一ID（MAC地址或者UUID）
linux的话，dmidecode命令查看设备uuid
ipconfig /all
Windows IP 配置

洞鉴（X-Ray）资产发现
- 主动资产发现
- 被动资产发现
  - 洞鉴（X-Ray）的被动资产发现，从代理、流量、日志三种方式被动地进行资产采集，是对主动资产发现的完美补充。
  - 基于代理的被动资产发现
    通过 HTTP 代理的方式，对关注的资产信息进行定制化采集，既快又准。
  - 基于流量的被动资产发现
    通过流量镜像的方式，导入pcap日志或者通过指定网卡实时检测流量，从实时流量中动态获取/更新资产，获取全接口请求，使得资产信息采集达到高的覆盖率。
  - 基于日志的被动资产发现
    基于日志的被动资产发现，支持直接导入 syslog 日志或者类似雷池（SafeLine）等 WAF 产品的日志，快速的对资产信息进行采集。
洞鉴（X-Ray）在进行资产采集时，能够遨游整个网络空间，全面探测授权范围内的设备和网站资产。通过强大的资产指纹库建立各类型资产的特征，包括网络设备、安全设备、各类操作系统、数据库、应用中间件，并且会对一些特定服务加强探测，对每个资产(域名、主机、服务和web 站点的资产详细信息)的结果进行多样化的信息展示。

4. 场景化优化：僵木蠕、挖矿、勒索：就是想想我们系统里面针对这三个场景，还需要做哪些优化、深化，才能让客户感兴趣，这块多半要基于情报的数据来做
流量和终端的数据拉通: 关联流量和终端只能用源目的地址、主机ip 或mac地址关联
得到网卡的MAC地址，然后用RARP协议获取MAC对应的IP
ARP: 地址解析协议，是根据IP地址获取物理地址的一个TCP/IP协议。主机发送信息时将包含目标IP地址的ARP请求广播到局域网络上的所有主机，并接收返回消息，以此确定目标的物理地址
RARP: 反向地址转换协议，允许局域网的物理机器从网关服务器的 ARP 表或者缓存上请求其 IP 地址。

## 一体机v3.6剩余问题汇总
- 告警高亮功能：有些规则没有输出威胁特征或威胁特征列表，这个需要让安全同事加一下
- 部分攻击场景名称改动，影响攻击场景数据表，sae规则数据表，ice规则数据表
sql update:
```
update attack_scene set name =REPLACE(name,'告警','') where name like "%告警";
INSERT INTO `attack_scene` (`id`, `parent_id`, `name`, `id_path`, `description`, `scene_type`, `scene_model`) VALUES ('Z8FA92791209', NULL, 'UEBA异常', '/Z8FA92791209', 'UEBA异常', 'xdr', 'xdr');
update sae_rule set sim_name = replace(sim_name,'告警',''), alarm_attr = JSON_REPLACE(alarm_attr,'$.simName', replace(JSON_UNQUOTE(JSON_EXTRACT(alarm_attr,'$.simName')),'告警','')), raw = JSON_REPLACE(raw,'$.simName', replace(JSON_UNQUOTE(JSON_EXTRACT(raw,'$.simName')),'告警','')) where sim_name like "%告警";
update ice_rule set source = replace(source,'威胁情报告警','威胁情报'), statement = replace(statement,'威胁情报告警','威胁情报'), raw = replace(raw,'威胁情报告警','威胁情报') where source like "%威胁情报告警%";
```